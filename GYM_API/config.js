const config = {
    db: {
      host: "localhost",
      user: "palestrauser",
      password: "Michele03!",
      database: "Palestra",
      connectTimeout: 60000
    }
  };

  module.exports = config;